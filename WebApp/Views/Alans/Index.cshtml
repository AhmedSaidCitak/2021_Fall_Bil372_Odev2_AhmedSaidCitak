@model IEnumerable<WebApp.Models.Alan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AlanAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AlanTipi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AlanAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AlanTipi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AlanID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AlanID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AlanID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
